logging:
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
spring:
  rabbitmq:
    host: ubuntu.wsl
    port: 5672
    username: aidan
    password: 123456
    virtual-host: /
    listener:
      simple:
        prefetch: 1 # 每次获取一条，防止预取多条来不及处理