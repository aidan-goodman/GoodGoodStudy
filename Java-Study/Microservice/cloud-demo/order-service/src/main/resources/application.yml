server:
  port: 8080
spring:
# 与 bootstrap 重复
#  application:
#    name: orderservice
#  cloud:
#    nacos:
#      server-addr: localhost:8848
#      discovery:
#        cluster-name: SD    # 搭建山东集群
        # namespace: 2fc0909a-aefe-40ab-9fdb-9bb83bbdc323 # 指定命名空间
        # ephemeral: false # 设置为非临时实例
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_order?characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  type-aliases-package: cn.itcast.user.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
#eureka:
#  client:
#    service-url:
#      defaultZone: http://127.0.0.1:10086/eureka
### 针对单个服务的 Ribbon 配置

#userservice:
#  ribbon:
#    # 基于配置文件形式的针对单个服务的 Ribbon 负载均衡策略 —— 随机策略
#    # NFLoadBalancerRuleClassName: com.netflix.loadbalancer.RandomRule
#    # Nacos 的规则 —— 优先集群，集群内随机
#    NFLoadBalancerRuleClassName: com.alibaba.cloud.nacos.ribbon.NacosRule
#ribbon:
#  # 使用饥饿加载策略
#  eager-load:
#    enabled: true
#    clients:
#      - userservice
